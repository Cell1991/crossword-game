# Test Scenarios — WordX (Crossword Multiplayer)

เอกสารนี้รวม test scenario ของเกมทั้งฝั่ง backend (กติกา, API, turn, HP, การ์ด, realtime, การเปลี่ยนตัวอักษร) และฝั่ง UI
ทุก scenario มีรหัส (เช่น `MV-03`) ซึ่งตรงกับชื่อฟังก์ชันเทสต์อัตโนมัติ (เช่น `test_mv03_...`)

กติกาอ้างอิง: `Unified_Online_Crossword_Rules.md` (เรื่องที่ไม่ได้เขียนไว้ใช้แบบ Letter League ใน Discord) ส่วนที่ยังไม่ได้ทำดูหัวข้อ [กติกาที่ยังไม่ได้ทำ](#กติกาที่ยังไม่ได้ทำ)

## วิธีรันเทสต์อัตโนมัติ

```bash
cd backend
pip install -r requirements.txt
python -m pytest -rxX
```

- `tests/conftest.py` บังคับให้เทสต์ใช้ SQLite ชั่วคราวเสมอ เพราะเทสต์ API จะ drop/create ตารางทุกครั้ง จึงไม่ไปลบข้อมูลใน `crossword.db` หรือ Postgres ใน docker
  (ถ้าต้องการใช้ฐานข้อมูลอื่นที่ทิ้งได้ ให้ตั้ง `TEST_DATABASE_URL`)
- `GameTable` ใน conftest ใช้จัดฉากให้ผลลัพธ์แน่นอน เช่น กำหนดตัวอักษรในแร็ก/ถุง (`set_tiles`), วางตัวอักษรบนกระดาน (`set_board`), ปรับ HP หรือจำนวนเทิร์น
- ผลล่าสุด: **114 passed** (แก้ BUG-1 แล้วจึงไม่มี xfailed เหลือ)

สัญลักษณ์: ✅ ผ่าน · ❌ พบบั๊ก (เทสต์เป็น `xfail(strict=True)` จะเปลี่ยนเป็น fail ทันทีเมื่อแก้บั๊กแล้ว ให้ถอด mark ออก) · 🖐 ยังไม่ได้ทดสอบมือ

กระดาน: **19 แถว × 27 คอลัมน์** ดาวกลางอยู่ที่ (9, 13) ช่องพิเศษ (3L 8 ช่อง, 2L 12 ช่อง, ⚡ 8 ช่อง) คือแบบ 15 × 15 เดิมกระจายตามสัดส่วน
ตำแหน่งอยู่ใน `backend/app/game/board.py` และ `frontend/lib/board.ts` (ต้องแก้ให้ตรงกันทั้งสองไฟล์)

---

## 1. Rules engine (`tests/test_game_engine.py`)

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| — | ขนาดกระดานและช่องพิเศษ | ตรวจขอบเขต 19 × 27 และตำแหน่งช่องพิเศษ | (18, 26) อยู่ในกระดาน, (19, 0) และ (0, 27) อยู่นอก, ช่องพิเศษ = แบบ 15 × 15 กระจายตามสัดส่วน, จำนวนเท่าเดิม, สมมาตรซ้าย-ขวา | ✅ |
| EN-01 | ถุงตัวอักษรเริ่มต้นถูกต้อง | สร้างถุงใหม่ | 100 ตัวรวม blank 2 ตัว, จำนวนแต่ละตัวตาม `DEFAULT_LETTER_FREQUENCIES`, ค่าคะแนนตาม `DEFAULT_LETTER_VALUES`, id ไม่ซ้ำ | ✅ |
| EN-02 | จั่วเกินจำนวนในถุง | ถุงมี 2 ตัว ขอจั่ว 7 | ได้ 2 ตัว ถุงว่าง | ✅ |
| EN-03 | วางผิดรูปแบบ | วางไม่อยู่แนวเดียว / นอกกระดาน / ช่องเดียวกันซ้ำ / ช่องที่มีตัวอักษรแล้ว | ปฏิเสธพร้อมเหตุผลที่ตรงกรณี | ✅ |
| EN-04 | ตัวเดียวสร้างคำ 2 แนว | กระดานมี A ที่ (7,6), O ที่ (8,7) วาง T ที่ (7,7) | ได้คำ AT และ TO คะแนนรวม 4 | ✅ |
| EN-05 | cross-word ผิดทำให้ทั้ง move ผิด | เหมือน EN-04 แต่พจนานุกรมไม่มี TO | ปฏิเสธ `'TO' is not recognized` | ✅ |
| EN-06 | โบนัสวางครบ 7 ตัว | คำ 7 ตัวไม่มีช่องพิเศษ | 7 ตัว = 7+50 = 57, 6 ตัว = ไม่มีโบนัส | ✅ |
| EN-07 | ผู้ชนะต้องยังมี HP | p1 คะแนนสูงแต่ HP 0, p2 HP เหลือ | เกมจบ ผู้ชนะคือ p2 | ✅ |
| EN-08 | ถุงไม่พอแยกจาก error อื่น | ขอเปลี่ยน 8 ตัว ถุงมี 7 | ได้ `NotEnoughTilesInBag` (ใช้ตัดสินว่านับเป็น pass) | ✅ |
| — | เทสต์เดิม | ตัวคูณเฉพาะตัวที่เพิ่งวาง, พจนานุกรม, คำแรกต้องทับดาวกลาง (9, 13), cross-word, ต้องเชื่อมกับกระดาน, ห้ามมีช่องว่าง, เงื่อนไขจบเกม, โหลด CSW24 | ตามชื่อเทสต์ | ✅ |

## 2. Lobby และการเริ่มเกม (`tests/test_game_scenarios.py`)

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| LB-01 | Host สร้างห้อง | `POST /api/rooms` ตั้งเวลา 60 วิ | ได้ PIN ตัวเลข 6 หลัก, ห้อง `WAITING`, มี host 1 คน, เก็บเวลาต่อเทิร์น | ✅ |
| LB-02 | ตั้งค่าห้องผิด | ชื่อว่าง / ชื่อยาวเกิน 32 / เวลา 45 วิ | 422 | ✅ |
| LB-03 | PIN ไม่มีอยู่จริง | join ด้วย `000000` | 404 | ✅ |
| LB-04 | ห้องเต็ม | มีผู้เล่น 6 คนแล้ว คนที่ 7 join | 400 "Room is full" | ✅ |
| LB-05 | join หลังเกมเริ่ม | เริ่มเกมแล้วมีคน join | 400 | ✅ |
| LB-06 | สิทธิ์เริ่มเกม | คนที่ไม่ใช่ host กดเริ่ม / host กดเริ่มซ้ำ | 403 / 400 | ✅ |
| LB-07 | แจกตัวอักษรตอนเริ่ม | 2 คน และ 3 คน | ทุกคนได้ 7 ตัว, ถุงเหลือ 100−7n, host เล่นก่อน, max_turns 20 (2 คน) / 21 (3 คน), มองไม่เห็นแร็กคนอื่น | ✅ |
| LB-08 | เล่นคนเดียว | เริ่มเกมคนเดียว วางคำ แล้ว exchange แล้ว pass | เกมไม่จบ ตากลับมาที่ตัวเอง (turn 3) จบเมื่อไม่ได้คะแนน 4 ครั้งติดกัน ผู้ชนะคือผู้เล่นคนนั้น | ✅ (แก้ BUG-8 แล้ว) |
| LB-09 | ออกจากล็อบบี้ | 3 คน Bob กด Leave lobby แล้ว Dan เข้ามาแทน แล้วเริ่มเกม | Bob หายจากห้อง ลำดับที่นั่งไม่ซ้ำ (0, 1, 2) Bob ไม่ได้ตา | ✅ (แก้ BUG-12 แล้ว) |
| LB-10 | host ออกจากล็อบบี้ | host กด Leave lobby | คนถัดไปเป็น host แทนและเริ่มเกมได้ host เดิมเริ่มไม่ได้ (403) | ✅ |

## 3. การวางคำ (Moves)

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| MV-01 | คำแรกต้องทับดาวกลาง | วาง CAT ที่มุมกระดาน | validate `valid=false` "center star", commit 400, กระดานว่าง | ✅ |
| MV-02 | คำแรกต้อง ≥ 2 ตัว | วาง A ตัวเดียวที่ (9,13) | ปฏิเสธ "at least 2 letters" | ✅ |
| MV-03 | คำแรกถูกต้อง | วาง CAT ที่ (9,12)–(9,14) | ได้ 5 คะแนน, คู่แข่ง HP 95, แร็กเติมกลับเป็น 7, ถุง −3, เปลี่ยนตาเป็นอีกคน, broadcast `MOVE_COMMITTED` | ✅ |
| MV-04 | คำไม่มีในพจนานุกรม | วาง CTA | validate "not recognized", commit 400, แร็ก/กระดาน/ตาไม่เปลี่ยน | ✅ |
| MV-05 | ใช้ตัวอักษรที่ไม่มีในแร็ก | ส่ง Z ปลอม | 400 "not in your rack" | ✅ |
| MV-06 | ยังไม่ถึงตา: เช็กคำได้แต่วางจริงไม่ได้ | Bob validate CAT ตอนตา Alice แล้วลอง commit | validate `valid=true` +5, commit 403, กระดานว่าง | ✅ |
| MV-07 | คำต่อไปต้องเชื่อมกระดาน | มี CAT แล้ว วาง DOG ไกลออกไป | ปฏิเสธ "must connect" | ✅ |
| MV-08 | ต่อคำเดิม | มี CAT แล้ว วาง S ที่ (9,15) | ได้คำ CATS คะแนน 6 | ✅ |
| MV-09 | ช่อง 2L + โบนัส 7 ตัว | วาง RETAINS แนวตั้ง (6,13)–(12,13) | S บน 2L (12,13) ได้ ×2 → 8 + 50 = 58, คู่แข่ง HP 42 | ✅ |
| MV-10 | ช่อง ⚡ ให้การ์ด | มี T ที่ (6,7) วาง A ที่ (5,7) | ได้การ์ด 1 ใบจาก `CARD_TYPES` | ✅ |
| MV-11 | ปลอมค่าคะแนนตัวอักษร | ส่ง C,A,T ที่ `value: 99` ทั้ง validate และ commit | คิด 5 คะแนนตามค่าจริง คู่แข่ง HP 95 กระดานเก็บค่าจริง | ✅ (แก้ BUG-1 แล้ว) |
| MV-12 | คะแนนรายคำรวมตัวคูณ | วาง RETAINS ให้ S ตก 2L | `words_formed` ของ RETAINS = 8 (ไม่รวมโบนัส 50) | ✅ (แก้ BUG-18 แล้ว) |

## 4. HP และการน็อกเอาต์

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| HP-01 | คะแนน = ดาเมจทุกคู่แข่ง | 3 คน Alice ได้ 5 คะแนน | Bob, Carol HP 95; Alice 100 | ✅ |
| HP-02 | น็อกคู่แข่งคนสุดท้าย | Bob HP 3, Alice ได้ 5 | เกมจบ, ผู้ชนะ Alice, Bob HP 0, broadcast `GAME_ENDED` | ✅ |
| HP-03 | คนที่ HP 0 ถูกข้ามตา | 3 คน Bob HP 0 | หลัง Alice เล่น ตาถัดไปคือ Carol | ✅ |

## 5. เทิร์น การผ่าน และการจบเกม

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| TN-01 | Pass | Alice กด pass | ตาเป็น Bob, turn 2, consecutive_passes 1, บันทึก move `PASS`, Alice pass ซ้ำได้ 403 | ✅ |
| TN-02 | เทิร์นไม่ได้คะแนนติดกัน 4 ครั้ง | 2 คน pass สลับกัน 4 ครั้ง | เกมจบที่ครั้งที่ 4 | ✅ |
| TN-03 | วางคำรีเซ็ตตัวนับ | Alice pass แล้ว Bob วางคำ | consecutive_passes = 0 | ✅ |
| TN-04 | ครบจำนวนเทิร์น | max_turns = 2 แล้ว pass 2 ครั้ง | เกม `FINISHED`, ไม่มีผู้เล่นปัจจุบัน | ✅ |
| TN-05 | หมดเวลาแล้วข้ามตา | ห้อง 30 วิ, เริ่มเทิร์นไปแล้ว 31 วิ, เรียก `/timeout` | ตาเปลี่ยนเป็น Bob (ก่อนหมดเวลาไม่เปลี่ยน) | ✅ |
| TN-06 | แจ้งผลการหมดเวลา | เหมือน TN-05 | response `expired: true` และ broadcast `TURN_PASSED` (reason `TIMEOUT`) | ✅ (แก้ BUG-2 แล้ว) |
| TN-07 | ห้องไม่จำกัดเวลา | เริ่มเทิร์นไปแล้ว 1 ชม. | `/timeout` ไม่ข้ามตา | ✅ |
| TN-08 | จบเพราะครบเทิร์นต้องมีผู้ชนะ | max_turns = 1 Bob มี 10 คะแนน Alice วางคำ (5) หรือ pass | `winner_id` = Bob ทั้งใน response, `GAME_ENDED` และสถานะเกม | ✅ (แก้ BUG-10 แล้ว) |
| TN-09 | pass เทิร์นสุดท้ายบันทึกเลขเทิร์นถูก | max_turns = 1 แล้ว pass | move `PASS` มี turn_number 1 | ✅ (แก้ BUG-19 แล้ว) |
| TN-10 | ไม่ได้คะแนนครบ 2 ตาต่อคน | 3 คน pass/exchange ติดกัน | 5 ครั้งยังไม่จบ ครั้งที่ 6 จบ (2 คนยังเป็น 4 ครั้ง) | ✅ (แก้ BUG-21 แล้ว) |

## 6. การออกจากเกม

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| LV-01 | ออกนอกตาตัวเอง | 3 คน Bob ออกตอนตา Alice | ตายังเป็น Alice, Bob `OFFLINE`, หลัง Alice pass ตาเป็น Carol | ✅ |
| LV-02 | เหลือคนเดียว | 2 คน Bob ออก แล้ว Alice pass | เกมจบ ผู้ชนะ Alice | ✅ |
| — | ออกตอนเป็นตาตัวเอง | (`tests/test_api.py`) | ตาเปลี่ยนเป็นคนถัดไป | ✅ |
| LV-03 | คนกลางออกตอนเป็นตาตัวเอง | 3 คน ตา Bob แล้ว Bob กด Exit | ตาเป็น Carol (เดิมกลับไป Alice) | ✅ (แก้ BUG-9 แล้ว) |
| LV-04 | คนที่ออกไปแล้วชนะไม่ได้ | Alice 50 คะแนนแล้วออก Bob/Carol pass จนจบ | ผู้ชนะคือ Carol (5 คะแนน) ไม่ใช่ Alice | ✅ (แก้ BUG-11 แล้ว) |

## 7. การ์ดพลังพิเศษ

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| CD-01 | ใช้การ์ดที่ไม่มี | ใช้ HEAL โดยไม่มีการ์ด | 400 | ✅ |
| CD-02 | DRAW_TILE | มีการ์ด DRAW_TILE แล้วใช้ | แร็ก 8 ตัว, ถุง −1, การ์ดหายไป | ✅ |
| CD-03 | BAN_LETTER | Alice แบนตัว A แล้ว pass, Bob วาง CAT | 400 "banned" | ✅ |
| CD-04 | คน HP 0 ใช้การ์ดไม่ได้ | Bob HP 0 ใช้ HEAL | 403 HP ยังเป็น 0 การ์ดยังอยู่ | ✅ (แก้ BUG-15 แล้ว) |
| CD-05 | ใช้การ์ดหลังจบเกมไม่ได้ | เกม `FINISHED` แล้วใช้ DESTROY_TILE | 400 กระดานไม่เปลี่ยน | ✅ (แก้ BUG-15 แล้ว) |

## 8. Realtime sync และการหลุดการเชื่อมต่อ

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| RT-01 | แจ้งเตือนหลังบันทึกเสร็จ | pass / exchange / leave / วางคำ แล้วให้ "เครื่องอื่น" โหลดสถานะทันทีที่ได้ยิน event | เห็นว่าเป็นตาของ Bob แล้วทุกกรณี (เดิมยังเห็นตาคนเก่า = สาเหตุตาค้าง) | ✅ (แก้ BUG-6 แล้ว) |
| RT-02 | หลุดแล้วไม่กลับมา | WebSocket ของ Alice หลุดและไม่ต่อใหม่ภายในเวลาผ่อนผัน | Alice `DISCONNECTED` (ไม่ใช่ออกจากเกม), ตาเป็น Bob, เกมยังเล่นต่อ, broadcast `PLAYER_DISCONNECTED` | ✅ |
| RT-03 | รีเฟรชหน้า | socket เก่าหลุดแต่ socket ใหม่ต่อแล้วภายในเวลาผ่อนผัน | Alice ยัง `ONLINE` และยังเป็นตา Alice, socket ใหม่ไม่ถูกลบ | ✅ (แก้ BUG-5 แล้ว) |
| RT-04 | มีคนต่อ socket ระหว่าง broadcast | socket ใหม่เข้ามาขณะกำลังส่ง event | ส่งครบทุกคน ไม่ error `dictionary changed size` | ✅ (แก้ BUG-16 แล้ว) |
| RT-05 | preview ผิดรูปแบบ | ส่ง `PLACEMENT_PREVIEW` ที่ tile ไม่มี row/col | คนอื่นยังอยู่ในรายชื่อรับ event ได้เฉพาะตำแหน่งที่ถูกต้อง | ✅ (แก้ BUG-17 แล้ว) |
| RT-06 | timestamp ของ event | สร้าง event 2 ครั้งห่างกัน | timestamp ไม่ซ้ำกัน | ✅ (แก้ BUG-4 แล้ว) |
| RT-07 | คู่แข่งหลุดไม่ทำให้ชนะทันที | 2 คน Bob หลุดเกินเวลาผ่อนผัน แล้ว Alice วางคำหรือ exchange | เกมไม่จบ ตากลับมาที่ Alice พอ Bob ต่อกลับมาก็ได้ตาตามปกติ | ✅ (แก้ BUG-22 แล้ว) |
| RT-08 | เล่นคนเดียวแล้วเน็ตหลุด | เกมคนเดียว socket ของ Alice หลุดเกินเวลาผ่อนผัน | เกมยังเล่นอยู่ ตายังเป็นของ Alice พอต่อกลับมาเล่นต่อได้ | ✅ (แก้ BUG-29 แล้ว) |

## 8.1 ผู้ชม (Spectator)

เข้าจากหน้าแรก → Join Game → ใส่ PIN → **Watch as spectator** (ไม่ต้องใส่ชื่อ) เข้าได้ทั้งตอนรอในล็อบบี้และระหว่างเกม ไม่นับเป็นผู้เล่น ไม่มีแร็ก ไม่ได้ตา
เห็นกระดาน ตา คะแนน เวลา แบบ realtime (WebSocket `?spectate=1` ไม่ต้องมี token) ตัวอักษรที่คนเป็นตาวางค้างไว้เห็นแค่ตำแหน่ง ทุกเครื่องเห็นจำนวนผู้ชม 👁 N

| ID | Scenario | ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| SP-01 | ผู้ชมไม่มีที่นั่ง | ต่อ WebSocket แบบผู้ชมเข้าเกม 2 คน แล้ว broadcast | ได้รับ event, `spectator_count` = 1, ผู้เล่นยังเป็น 2 คนสถานะเดิม, ออกแล้ว count กลับเป็น 0 | ✅ |
| SP-02 | ดูเกมที่ไม่มีอยู่ | ต่อแบบผู้ชมด้วย id ปลอม | ปิด socket code 4004 | ✅ |
| SP-03 | สถานะเกมบอกเลขห้อง | `GET /games/{id}` ไม่มี token | มี `game_pin` และไม่มีแร็กของใคร | ✅ |
| UI-SP-01 | ดูระหว่างเกม | ใส่ PIN กด Watch as spectator | เห็นกระดาน, PIN, 👁 1, ScoreBoard, แถบล่าง "You are watching this game" ไม่มีแร็ก; Alice pass แล้วภายใน 1 วิ ขึ้น "NEXT: Bob TURN 4" | ✅ |
| UI-SP-02 | ดูตั้งแต่ล็อบบี้ | Watch ห้องที่ยังไม่เริ่ม แล้ว host กดเริ่ม | ล็อบบี้ไม่นับเป็นผู้เล่น ขึ้น "👁 Watching" และ "Stop watching", พอเริ่มเกมเข้าหน้ากระดานเอง | ✅ |

---

## 9. ระบบเปลี่ยนตัวอักษรกับถุง (Exchange)

### กติกา (ตาม §5–6 ของไฟล์กติกา)

- ใช้ได้เฉพาะ **ตาของตัวเอง** เกมต้องอยู่สถานะ `PLAYING` และผู้เล่นต้องมี HP > 0 และไม่ `OFFLINE`
- ถุงต้องเหลือตัวอักษร **อย่างน้อย 7 ตัว** ถึงจะเปลี่ยนได้
- เลือกได้ตั้งแต่ 1 ตัวจนถึงทั้งแร็ก ถ้าขอเปลี่ยนมากกว่าที่ถุงมี (เกิดได้เมื่อแร็กเกิน 7 ตัวจากการ์ด) **นับเป็น pass**
- ระบบ **จั่วตัวใหม่ก่อน** แล้วค่อยใส่ตัวเก่ากลับลงถุงและสับ จึงไม่มีทางจั่วได้ตัวที่เพิ่งคืนไป ตัวใหม่เข้าที่ช่องเดิมของตัวที่ถูกเปลี่ยน
- การเปลี่ยน **เสียตา** ไม่ได้คะแนน ไม่ทำดาเมจ บันทึก move `EXCHANGE` และ **นับเป็นเทิร์นที่ไม่ได้คะแนน** (รวมกับ pass ครบ 2 ตาต่อคนที่ยังเล่นอยู่ติดกันเกมจบ ขั้นต่ำ 4 ครั้ง ดู TN-10)
- ถ้าเป็นเทิร์นสุดท้าย (`max_turns`) เกมจบ
- API: `POST /api/games/{game_id}/exchange` header `X-Player-ID`, body `{"tile_ids": ["..."]}` ตอบ `status: "exchanged"` หรือ `"passed"`
- WebSocket: `TILES_EXCHANGED` ส่งแค่ `playerId`, `count`, `nextPlayerId`, `turnNumber` ไม่เปิดเผยว่าเปลี่ยนตัวอะไร

### Backend (`tests/test_tile_exchange.py`)

| ID | Scenario | เงื่อนไข / ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| EX-01 | ตัวใหม่เข้าที่ช่องเดิม | แร็ก ABCDEFG เปลี่ยนตัวที่ 2 และ 4, ถุง XYZ | แร็ก AXCYEFG, ถุงมี Z B D | ✅ |
| EX-02 | ไม่จั่วตัวเดิมกลับมา | เปลี่ยน QQQ, ถุงมี EEE | ได้ EEE | ✅ |
| EX-03 | ตัวอักษรไม่หายไม่งอก | ถุงจริง 100 ตัว เปลี่ยน 4 ตัว | ขนาดแร็ก/ถุงเท่าเดิม, ชุด id รวมเท่าเดิม | ✅ |
| EX-04 | ถุงมีพอดี (ระดับฟังก์ชัน) | ถุง 2 ตัว เปลี่ยน 2 ตัว | สำเร็จ | ✅ |
| EX-05 | คำขอผิด (ระดับฟังก์ชัน) | ไม่เลือกเลย / id ซ้ำ / ไม่ใช่ตัวในแร็ก / ถุงไม่พอ / ถุงว่าง | `ValueError` พร้อมเหตุผล | ✅ |
| EX-10 | เปลี่ยนสำเร็จผ่าน API | Alice แร็ก QQQAEIO เปลี่ยน Q ทั้ง 3 | แร็ก STRAEIO, ถุงยัง 9 ตัวและมี Q 3 ตัว, ตาเป็น Bob, consecutive_passes 1, คะแนน/HP ไม่เปลี่ยน, move `EXCHANGE`, broadcast มีแค่จำนวน | ✅ |
| EX-11 | เปลี่ยนทั้งแร็ก | เปลี่ยน 7 ตัว | ได้ 7 ตัวใหม่ทั้งหมด ถุงยัง 84 | ✅ |
| EX-12 | แร็กเกิน 7 ตัว | แร็ก 8 ตัว ถุง 9 ตัว เปลี่ยนทั้งหมด | สำเร็จ | ✅ |
| EX-13 | เปลี่ยนนอกตา | Bob เปลี่ยนตอนตา Alice | 403 ไม่มีอะไรเปลี่ยน | ✅ |
| EX-14 | เปลี่ยนตัวของคู่แข่ง | Alice ส่ง id ตัวอักษรของ Bob | 400 ทั้งสองแร็กไม่เปลี่ยน | ✅ |
| EX-15 | คำขอผิดรูปแบบ | id ซ้ำ / list ว่าง | 400 / 422 ไม่มีอะไรเปลี่ยน | ✅ |
| EX-16 | ถุงต้องเหลือ ≥ 7 | ถุง 6 ตัว / ถุงว่าง / ถุง 7 ตัวเปลี่ยน 7 | 400 "at least 7 tiles", 400, 200 | ✅ |
| EX-17 | ขอมากกว่าที่ถุงมี = pass | แร็ก 8 ตัว ถุง 7 ตัว ขอเปลี่ยน 8 | ตอบ `passed`, แร็ก/ถุงไม่เปลี่ยน, ตาเป็น Bob, move `PASS`, broadcast `TURN_PASSED` | ✅ |
| EX-18 | เกมไม่ได้กำลังเล่น | ห้อง `WAITING` / เกม `FINISHED` | 400 | ✅ |
| EX-19 | HP 0 เปลี่ยนไม่ได้ | Alice HP 0 | 403 | ✅ |
| EX-20 | exchange นับเป็นเทิร์นไม่ได้คะแนน | pass, exchange, exchange, exchange ติดกัน | เกมจบที่ครั้งที่ 4 | ✅ |
| EX-21 | เปลี่ยนในเทิร์นสุดท้าย | max_turns = 1 | เกมจบ, broadcast `GAME_ENDED` | ✅ |
| EX-22 | ข้ามคนที่ออกไปแล้ว | 3 คน Bob ออก, Alice เปลี่ยน | ตาถัดไปคือ Carol | ✅ |

### UI

| ID | Scenario | ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| UI-EX-01 | ปุ่ม Exchange | ดูแถบเครื่องมือตอนเป็นตาตัวเอง | มีปุ่ม Exchange ถัดจาก Shuffle และกดได้ | ✅ |
| UI-EX-02 | ปุ่มถูกปิดเมื่อไม่ควรกด | ไม่ใช่ตาตัวเอง / มีตัวอักษรวางค้าง / ถุงเหลือไม่ถึง 7 / กำลังส่ง | ปุ่ม Exchange กดไม่ได้ | ✅ (ตรวจแล้วเฉพาะกรณีไม่ใช่ตาตัวเอง) |
| UI-EX-03 | เข้าโหมดเลือก | กด Exchange | แถบเครื่องมือเปลี่ยนเป็น Cancel + คำอธิบาย + ปุ่ม "Exchange 0 tiles" (กดไม่ได้) | ✅ |
| UI-EX-04 | เลือก/ยกเลิกเลือกตัวอักษร | แตะตัวอักษรในแร็ก แตะซ้ำ | ตัวที่เลือกยกขึ้นเป็นสีฟ้า, ตัวเลขบนปุ่มนับตาม, แตะซ้ำเพื่อยกเลิก | ✅ |
| UI-EX-05 | ลากไม่ได้ในโหมดเปลี่ยน | ลากตัวอักษรไปกระดาน | ไม่ยกขึ้นกระดาน | ✅ |
| UI-EX-06 | ยกเลิก | กด Cancel | กลับแถบเครื่องมือปกติ ไม่มีตัวไหนถูกเลือก แร็กไม่เปลี่ยน | ✅ |
| UI-EX-07 | ยืนยัน | เลือก 2 ตัวแล้วกด Exchange 2 tiles | ตัวที่เลือกถูกแทนด้วยตัวใหม่ในช่องเดิม, ตาเปลี่ยน, มีข้อความ "You exchanged 2 tiles", จำนวนในถุงเท่าเดิม | ✅ |
| UI-EX-08 | ฝั่งคู่แข่ง | อีกเครื่องดูอยู่ตอนเปลี่ยน | เห็น "Alice exchanged 2 tiles" และตาเป็นของตัวเอง แต่ไม่รู้ว่าเปลี่ยนตัวอะไร | ✅ |
| UI-EX-09 | แร็กเกิน 7 ตัวแต่ถุงไม่พอ | ได้ตัวอักษรเพิ่มจากการ์ดแล้วเลือกมากกว่าที่ถุงมี | ข้อความ "Only N left in the bag" และปุ่มยืนยันกดไม่ได้ | 🖐 |
| UI-EX-10 | หมดเวลาระหว่างเลือก | ห้องจับเวลา ปล่อยให้หมดเวลาตอนอยู่ในโหมดเลือก | ออกจากโหมดเลือกอัตโนมัติเมื่อตาเปลี่ยน | 🖐 |

## 10. UI ทั่วไป

| ID | Scenario | ขั้นตอน | ผลลัพธ์ที่คาดหวัง | สถานะ |
|---|---|---|---|---|
| UI-01 | สร้าง/เข้าห้องจากหน้าแรก | สร้างห้อง, อีกเครื่องใส่ PIN | เห็นรายชื่อผู้เล่นใน lobby | ✅ |
| UI-02 | วางตัวอักษรด้วยการคลิก | เลือกตัวในแร็กแล้วคลิกช่อง | ตัวอักษรไปอยู่บนช่อง ช่องในแร็กเว้นว่างไว้ | ✅ |
| UI-03 | ลากวาง | ลากจากแร็กไปกระดาน, ลากกลับแร็ก, ลากสลับบนกระดาน | ตำแหน่งถูกต้อง ไฮไลต์ช่องถูก/ผิดระหว่างลาก | 🖐 |
| UI-04 | สลับที่ในแร็ก / Shuffle | ลากสลับในแร็ก, กด Shuffle (กดได้แม้ไม่ใช่ตาตัวเอง) | ลำดับเปลี่ยน ช่องว่างอยู่ที่เดิม | 🖐 |
| UI-05 | Preview คะแนน | วางคำถูก / ผิด | คำถูก: ตัวอักษรเขียว + "+N pts" + ปุ่ม Confirm Move; คำผิด: แดง + ป้าย "Invalid word" + ข้อความเหตุผล และ**ไม่มีปุ่ม Confirm Move** | ✅ |
| UI-06 | Confirm Move / Cancel | วางแล้วกดยืนยัน หรือยกเลิก | ยืนยัน: ตัวอักษรติดกระดาน แร็กเติม ตาเปลี่ยน; ยกเลิก: ตัวกลับแร็ก | 🖐 |
| UI-07 | Pass | กด Pass | ตาเปลี่ยน ปุ่มกดไม่ได้ขณะมีตัววางค้าง | 🖐 |
| UI-08 | Preview ของคู่แข่ง | คนที่เป็นตาวางตัวอักษรค้างไว้ | เห็นแค่ตำแหน่ง ไม่เห็นตัวอักษร; ตัวที่คนอื่นวางซ้อมตอนไม่ใช่ตาจะไม่โชว์ | 🖐 |
| UI-09 | ตัวจับเวลา | ห้อง 30 วิ 3 คน | นับถอยหลังทุกเครื่อง หมดเวลาแล้วข้ามตาไปคนถัดไปเอง | ✅ |
| UI-09b | นาฬิกาเครื่องไม่ตรง server | ตั้งนาฬิกาเครื่องเร็วไป 10 วิ และให้ `/timeout` ครั้งแรกตอบ "ยังไม่หมด" | นับถอยหลังตามเวลา server หลังโหลดสถานะครั้งถัดไป, เรียก `/timeout` ซ้ำใน ~2 วิ แล้วข้ามตาได้ (เดิมค้างที่ 0s) | ✅ (แก้ BUG-14 แล้ว) |
| UI-10 | หลุดการเชื่อมต่อ | ปิด backend ชั่วคราว | แสดง "Reconnecting..." แล้วกลับมา "Live" | 🖐 |
| UI-11 | Exit ระหว่างตาตัวเอง | Carol กด Exit ตอนเป็นตา Carol | ทุกเครื่องเห็นตาเป็นคนถัดไปทันทีโดยไม่ต้องรีเฟรช, ชื่อ Carol ขึ้น "(Left)" | ✅ |
| UI-12 | จบเกม | Bob 60 คะแนนแต่ HP 0, Alice 0 คะแนน แล้วเกมจบ | หน้า Game Over ขึ้น "Alice wins!" ตามผู้ชนะจาก server (มี 🏆 ข้างชื่อ) คะแนนยังเรียงจากมากไปน้อย | ✅ (แก้ BUG-7 แล้ว) |
| UI-13 | จอเล็ก | เปิดบนมือถือ (~400px) | แถบเครื่องมือขึ้นบรรทัดใหม่ได้ ไม่ล้นจอ | ✅ |
| UI-14 | กระดาน 19 × 27 | เปิดเกม | กระดานกว้าง 27 ช่อง สูง 19 ช่อง ดาวกลางอยู่กลางจอ ซูมออกได้ถึง 30% | ✅ |
| UI-15 | วางซ้อมตอนตาคนอื่น | ตอนยังไม่ถึงตา วางคำลงกระดาน | บอกได้ว่าคำถูก/ผิด, ป้าย "PRACTICE: +N pts", Confirm กดไม่ได้, Cancel กดได้ | ✅ |
| UI-16 | ถึงตาตัวเอง | วาง TIX ไว้ตอนตา Bob (ป้าย "PRACTICE: +10 pts") แล้ว Bob pass | ตัวที่วางรอยังอยู่บนกระดาน ป้ายเปลี่ยนเป็น "PREVIEW: +10 pts" กด Confirm Move ได้ทันที (ทดสอบกดแล้วได้ 10 คะแนน) ตัวที่ช่องโดนคนอื่นวางทับหรือไม่อยู่ในแร็กแล้วเท่านั้นที่กลับแร็ก | ✅ (เปลี่ยนตามแชททีม: เดิมล้างกลับแร็กหมด) |
| UI-17 | คนอื่นวางคำทับช่องที่เราวางซ้อม | วางซ้อมไว้แล้วคนที่เป็นตาวางคำลงช่องเดียวกัน | ตัวที่ชนกลับเข้าแร็ก ตัวอื่นยังอยู่ | 🖐 |
| UI-18 | รีเฟรชหน้าระหว่างตาตัวเอง | กดรีเฟรชแล้วหน้ากลับมาภายใน 15 วิ | ยังเป็นตาตัวเอง ไม่ถูกนับว่าออก (ระดับ backend มี RT-03) | 🖐 |
| UI-19 | พลาด event แล้วยังไม่ค้าง | บล็อก WebSocket แล้วให้อีกคนเล่น | หน้าจอดึงสถานะใหม่เองภายใน ~5 วิ | 🖐 |
| UI-20 | ลากกระดานไม่ใช่คลิก | เลือกตัวในแร็กแล้วลากกระดานไปที่อื่นแล้วปล่อย | กระดานเลื่อน ตัวอักษรยังอยู่ในแร็ก ไม่ถูกวางลงช่องที่ปล่อยเมาส์ | ✅ (แก้ BUG-13 แล้ว) |
| UI-21 | ซูมตามเมาส์ | หมุนล้อเมาส์บนดาวกลาง 3 ครั้ง และห่างดาว 120 px อีก 1 ครั้ง | ดาวอยู่ใต้เมาส์ตลอด (คลาด ≤ 0.5 px) จุดอื่นเลื่อนตามสัดส่วนซูม | ✅ (แก้ BUG-23 แล้ว) |
| UI-22 | ซูมไม่ค้าง | หมุนล้อเมาส์แล้วกระดานรีเฟรช (poll 5 วิ) ก่อนเฟรมถัดไป แล้วหมุนต่อ | ซูมต่อได้ (เดิมซูมไม่ได้อีกเลยจนรีโหลดหน้า) | ✅ (แก้ BUG-24 แล้ว) |
| UI-23 | ออกจากล็อบบี้ | Bob เข้าห้องแล้วกด Leave lobby | รายชื่อฝั่ง host เหลือคนเดียว | ✅ (แก้ BUG-12 แล้ว) |
| UI-24 | เล่นคนเดียว | host เริ่มเกมคนเดียว วางคำ WE แล้ว exchange 1 ตัว | ปุ่ม Start กดได้ ("or start now to play solo"), หลังวางคำขึ้น "✨ WE (+5 pts)" และเป็น TURN 2, หลัง exchange เป็น TURN 3 เกมไม่จบ | ✅ (แก้ BUG-8, BUG-3 แล้ว) |
| UI-25 | ลากตัวอักษรขึ้นบนไม่ซ้ำ | ลากตัวจากแร็กขึ้นไปผ่านกระดาน แถวบนสุด จนถึงแถบบน (y = 15) ทั้งเมาส์และนิ้ว ทั้งจอ 1024 × 768 และ 375 × 812 | มีตัวลอยตัวเดียวอยู่ใต้นิ้วทุกระดับความสูง ปล่อยแล้วหายหมด (เดิมมีตัวที่ 2 ลอยอยู่ต่ำกว่านิ้ว 560 px) | ✅ (แก้ BUG-25 แล้ว) |
| UI-26 | แถบบนจอมือถือ | เปิดเกมที่ 375 px | แถวแรก Exit, โลโก้, จุดสถานะ, PIN, เวลา (∞), ถุง; แถวสองป้ายตา ไม่มีข้อความทับกัน | ✅ (แก้ BUG-26 แล้ว) |
| UI-27 | ข้อความลอยไม่ทับกัน | มี error ค้างแล้วอีกคน exchange | "✨ Bob exchanged 1 tile" กับ error เรียงเป็นกองบนกระดาน ไม่ทับกันและไม่บังปุ่มซูม | ✅ (แก้ BUG-27 แล้ว) |
| UI-28 | บอกเลขห้องในเกม | ดูแถบบน | มี "PIN 148192" กดแล้วคัดลอก | ✅ |
| UI-29 | ซูมอิสระ | หมุนล้อ/เลื่อน trackpad ทีละนิด, 1 notch, แบบบรรทัด | 100→102→103→105%, notch ≈ 14%, ไม่ติดขั้นละ 10% อีก ปุ่ม +/- ทีละ 20% | ✅ |
| UI-30 | ถ่างสองนิ้วซูม (มือถือ) | ถ่างนิ้วจาก 60 เป็น 120 px แล้วหุบ 120 → 90 px | 100→200% (เพดาน) แล้ว 150% จุดกึ่งกลางนิ้วอยู่ที่เดิม ไม่วางตัวอักษรหรือคลิกช่องโดยไม่ตั้งใจ | ✅ |

---

## บั๊กที่พบ

| ID | สถานะ | ความรุนแรง | รายละเอียด | ตำแหน่ง | เทสต์ |
|---|---|---|---|---|---|
| BUG-1 | ✅ แก้แล้ว | สูง (โกงได้) | Server เชื่อ `value` ที่ client ส่งมา ตรวจแค่ว่ามีตัวอักษรนั้นในแร็ก ผู้เล่นจึงส่ง `value: 99` เพื่อได้คะแนนและทำดาเมจเท่าไรก็ได้ ตอนนี้ใช้ค่าจากแร็กใน server เสมอ | `backend/app/services/move_service.py` | MV-11 |
| BUG-2 | ✅ แก้แล้ว | กลาง | `/timeout` ตอบ `expired: false` เมื่อหมดเวลาจริงและไม่ broadcast `TURN_PASSED` | `backend/app/services/game_service.py` | TN-06 |
| BUG-3 | ✅ แก้แล้ว | ต่ำ | ข้อความ "✨ คำที่ได้ (+N pts)" หลังวางคำไม่เคยแสดง เพราะ frontend อ่าน `words_formed` แต่ backend ส่ง `wordsFormed` | `frontend/app/game/[gameId]/page.tsx` | UI-24 |
| BUG-4 | ✅ แก้แล้ว | ต่ำ | `WebSocketEvent.timestamp` คำนวณครั้งเดียวตอน import ทุก event จึงมีเวลาเดียวกัน | `backend/app/schemas/events.py` | RT-06 |
| BUG-5 | ✅ แก้แล้ว | กลาง | WebSocket หลุดแล้วต่อใหม่ (เช่น รีเฟรช) ระหว่างตาตัวเอง ทำให้เสียตาทันทีและสถานะค้าง `OFFLINE` ตอนนี้รอ 15 วิ (`DISCONNECT_GRACE_SECONDS`) ถ้าต่อกลับมาก็ไม่นับว่าออก และ socket เก่าที่ปิดทีหลังจะไม่ลบ socket ใหม่ | `backend/app/websocket/handlers.py`, `connection_manager.py` | RT-02, RT-03 |
| BUG-6 | ✅ แก้แล้ว | สูง | **สาเหตุ "ตาค้าง" จากแชท:** server แจ้ง event ก่อน commit ลงฐานข้อมูล เครื่องอื่นโหลดได้สถานะเก่าแล้วค้างที่ตาคนเดิม (เช่น คนที่กด Exit ไปแล้ว) ตอนนี้ commit ก่อนแจ้งทุก endpoint และหน้าเกมดึงสถานะใหม่ทุก 5 วิเผื่อพลาด event | `backend/app/api/*.py`, `frontend/app/game/[gameId]/page.tsx` | RT-01, UI-11 |
| BUG-7 | ✅ แก้แล้ว | กลาง | หน้า Game Over เลือกผู้ชนะจากคะแนนสูงสุด ไม่สนผู้ชนะที่ server ส่งมา ตอนนี้ server เก็บ `games.winner_id` (เพิ่มคอลัมน์ผ่าน `_upgrade_existing_schema`) และส่งใน `GET /games/{id}` หน้า Game Over ใช้ค่านี้ | `page.tsx`, `game_service.py`, `models.py` | UI-12, TN-08 |
| BUG-8 | ✅ แก้แล้ว | กลาง | เริ่มเกมคนเดียวได้ แต่วางคำ/exchange/pass ครั้งแรกเกมจบทันทีและบังคับชนะ ตอนนี้เกมคนเดียวเล่นต่อได้ (ตากลับมาที่ตัวเอง) จนครบเงื่อนไขจบเกมจริง | `game_service.py`, `move_service.py` | LB-08, UI-24 |
| BUG-9 | ✅ แก้แล้ว | สูง | คนกลางกด Exit ตอนเป็นตาตัวเอง (3 คนขึ้นไป) → ตาย้อนกลับไปคนแรกแทนคนถัดไป เพราะระบบย้ายคนที่ออกไปไว้หัวคิว | `game_service.py` (`next_player_after`) | LV-03 |
| BUG-10 | ✅ แก้แล้ว | กลาง | เกมจบเพราะครบเทิร์น หรือเหลือผู้เล่นคนเดียวตอนวางคำ → `winner_id` เป็น null | `game_service.py` (`finish_game`), `move_service.py` | TN-08 |
| BUG-11 | ✅ แก้แล้ว | กลาง | คนที่กด Exit ไปแล้วแต่คะแนนสูงสุดถูกประกาศเป็นผู้ชนะ | `game_end.py` (`determine_winner`) | LV-04 |
| BUG-12 | ✅ แก้แล้ว | สูง | ปุ่ม "Leave lobby" แค่เปลี่ยนหน้า ชื่อยังค้างในห้อง พอเริ่มเกมคนที่ออกไปได้ตา ห้องไม่จับเวลาจะค้างถาวร ตอนนี้มี `POST /rooms/{pin}/leave` (host ออก → โอน host) และล็อบบี้ดู host จาก server | `room_service.py`, `rooms.py`, `lobby/[pin]/page.tsx` | LB-09, LB-10, UI-23 |
| BUG-13 | ✅ แก้แล้ว | กลาง | ลากกระดานแล้วปล่อย ถูกนับเป็นคลิกช่อง → วางตัวที่เลือกไว้ลงช่องนั้น หรือดึงตัวที่วางไว้กลับแร็ก (วัดระยะจากจุดล่าสุดแทนจุดที่กด) | `BoardCanvas.tsx` | UI-20 |
| BUG-14 | ✅ แก้แล้ว | กลาง | ตัวจับเวลาค้างที่ 0s ถ้านาฬิกาเครื่องเร็วกว่า server (เรียก `/timeout` ครั้งเดียวต่อเทิร์น) และนับถอยหลังด้วยนาฬิกาเครื่อง ตอนนี้ใช้ `server_time` จาก API และลองใหม่ทุก 2 วิ | `page.tsx`, `schemas/game.py` | UI-09b |
| BUG-15 | ✅ แก้แล้ว | กลาง | คน HP 0 ใช้ HEAL ฟื้นได้ และใช้การ์ดได้หลังเกมจบ | `cards.py` | CD-04, CD-05 |
| BUG-16 | ✅ แก้แล้ว | กลาง | มีคนต่อ/หลุด socket ระหว่าง broadcast → `RuntimeError: dictionary changed size` คนที่กดเห็น error ทั้งที่บันทึกแล้ว และ event ถัดไป (เช่น `GAME_ENDED`) ไม่ถูกส่ง | `connection_manager.py` | RT-04 |
| BUG-17 | ✅ แก้แล้ว | ต่ำ | `PLACEMENT_PREVIEW` ผิดรูปแบบจากเครื่องเดียวทำให้ทุกคนหลุดจากรายชื่อรับ event (และ JSON ที่ไม่ใช่ object ทำให้ลูป socket ตาย) | `connection_manager.py`, `handlers.py` | RT-05 |
| BUG-18 | ✅ แก้แล้ว | ต่ำ | คะแนนรายคำใน `words_formed` ไม่รวมตัวคูณ 2L/3L | `move_service.py` | MV-12 |
| BUG-19 | ✅ แก้แล้ว | ต่ำ | pass ในเทิร์นสุดท้ายบันทึก turn_number ผิด (0 แทน 1) | `game_service.py` | TN-09 |
| BUG-20 | ✅ แก้แล้ว | ต่ำ | ย้ายตัวอักษรแล้วผลตรวจเก่ายังค้าง/ผลที่มาช้าทับผลใหม่ ทำให้กด Confirm ด้วยผลตรวจของตำแหน่งเดิม | `page.tsx` | — |
| BUG-21 | ✅ แก้แล้ว | กลาง | **"กด exchange แล้วเกมจบ"**: เกม 3 คนขึ้นไปจบเมื่อไม่ได้คะแนน 4 ครั้งติดกัน ก่อนทุกคนได้เล่นครบ 2 ตา ตอนนี้ต้องครบ 2 ตาต่อคนที่ยังเล่นอยู่ (ขั้นต่ำ 4) ตามกติกา | `game_service.py` (`scoreless_turn_limit`) | TN-10 |
| BUG-22 | ✅ แก้แล้ว | สูง | **"วางคำ/exchange แล้วเกมจบ"**: คู่แข่งที่ socket หลุดเกิน 15 วิ (เช่น ล็อกจอมือถือ) ถูกนับว่าออกจากเกม (`OFFLINE`) พออีกคนเล่นเกมจบและชนะทันที ตอนนี้เป็น `DISCONNECTED` ข้ามตาไว้จนกว่าจะต่อกลับ เกมไม่จบ (ScoreBoard ขึ้น "(Away)") | `handlers.py`, `game_service.py` | RT-02, RT-07 |
| BUG-23 | ✅ แก้แล้ว | กลาง | ซูมไม่ตามเคอร์เซอร์: `zoomAtPoint` เรียก `setOffset` ข้างใน updater ของ `setScale` ซึ่ง React อาจเรียกซ้ำ (Strict Mode) กระดานจึงเลื่อนสองรอบ ตอนนี้เก็บ scale+offset ใน state เดียว และไม่นับการเลื่อนแนวนอนของ trackpad เป็นการซูม | `useBoardCamera.ts` | UI-21 |
| BUG-24 | ✅ แก้แล้ว | กลาง | ซูมใช้ไม่ได้อีกเลยจนรีโหลด: effect ของ render loop ยกเลิกเฟรมซูมที่รออยู่ทุกครั้งที่กระดานเปลี่ยน แต่ไม่ล้าง `wheelFrameRef` | `BoardCanvas.tsx` | UI-22 |
| BUG-25 | ✅ แก้แล้ว | กลาง | **"ลากไปข้างบนแล้วตัวซ้ำ"**: ตัวลอยของแร็กเป็น `position: fixed` แต่แถบล่างมี `backdrop-blur` ซึ่งทำให้ fixed อิงแถบนั้นแทนหน้าจอ ตัวลอยจึงไปโผล่ต่ำกว่านิ้วเท่าความสูงถึงแถบ (560 px บนมือถือ) ตอนลากขึ้นบนเลยเห็นสองตัว แก้โดย portal ไป `<body>` และซ่อนเมื่อกระดานรับช่วงลากต่อ | `TileRack.tsx` | UI-25 |
| BUG-26 | ✅ แก้แล้ว | ต่ำ | **"คำลอยซ้อนกัน"**: บนมือถือแถบบนไม่ตัดบรรทัด "Live" ทับป้าย YOUR TURN, ป้ายตาแตกสองบรรทัด, เวลาและถุงล้นจอ | `page.tsx`, `TurnBanner.tsx` | UI-26 |
| BUG-27 | ✅ แก้แล้ว | ต่ำ | **"คำลอยซ้อนกัน"**: ข้อความ error กับ "✨ …" อยู่ตำแหน่งเดียวกัน (`top-16`) จึงทับกันเมื่อขึ้นพร้อมกัน | `page.tsx` | UI-27 |
| BUG-28 | ✅ แก้แล้ว | ต่ำ | ข้อความ error จากการตรวจคำค้างอยู่หลังตัวอักษรกลับแร็กแล้ว (เช่น พอถึงตาตัวเองตัวที่วางซ้อมกลับแร็ก) | `page.tsx` | — |
| BUG-29 | ✅ แก้แล้ว | กลาง | (เกิดจากการแก้ BUG-22) เล่นคนเดียวแล้ว socket หลุดเกิน 15 วิ เกมจบเพราะไม่มีใครรับตาต่อ ตอนนี้ถ้าไม่มีใครรับตาได้จะเก็บตาไว้ให้คนที่หลุดจนกลับมา | `game_service.py` (`leave_game`) | RT-08 |

ข้อสังเกต (ควรตัดสินใจเชิงออกแบบ): `POST /cards/use` ยังใช้ได้นอกตาตัวเอง (ตอนนี้บังคับแค่เกมต้องกำลังเล่น และผู้ใช้ต้องยังมี HP และไม่ได้ออก) และไม่แจ้งผู้เล่นคนอื่นผ่าน WebSocket (อาศัยการดึงสถานะทุก 5 วิ) · คนที่ปิดแท็บล็อบบี้ไปเฉย ๆ (ไม่กด Leave) ยังได้ตาในเกม ถ้าห้องจับเวลาจะถูกข้ามเอง · จอเล็กกว่า `lg` ไม่มี ScoreBoard (ทั้งผู้เล่นและผู้ชมบนมือถือจึงไม่เห็นคะแนน/HP)

## รายการจากแชททีม

| รายการ | ผล |
|---|---|
| เวลาไม่นับ / ระบบจับเวลาของเทิร์น | host ยังเลือก 30/60/90/120 วิ หรือไม่จำกัดได้เหมือนเดิม แก้ BUG-2 และทดสอบในเบราว์เซอร์แล้วว่านับถอยหลังและข้ามตาเองทุกเครื่อง (UI-09) |
| ถุงเปลี่ยนตัวอักษร | ทำแล้ว ตามกติกา §5–6 (หัวข้อ 9) |
| กด Exit แล้วไม่ข้ามเทิร์น / ตาค้างที่คนที่ออก | แก้ BUG-6 และ BUG-5, คนที่ออกขึ้น "(Left)" (UI-11) |
| เปลี่ยนเทิร์นแล้วยังเป็นตาคนเดิม | แก้ BUG-6 (RT-01) |
| ตาคนที่ 3 ค้างที่คนที่ 2 และรีเฟรชแล้วกลับไปคนที่ 1 | ค้าง = BUG-6, รีเฟรชแล้วเสียตา = BUG-5 |
| วางคำรอไว้ตอนตาคนอื่น | เช็กได้ว่าคำใช้ได้ไหม (ป้าย PRACTICE) และพอถึงตาตัวเองตัวที่วางรอไว้ยังอยู่ กด Confirm ได้เลย (MV-06, UI-15, UI-16) |
| ตอนวางรอเทิร์นแล้วเข้าตาเรา ตัวที่วางรอไว้หาย | เปลี่ยนแล้ว: ไม่ล้างกลับแร็ก ตัวที่วางรอยังอยู่บนกระดานและตรวจคำใหม่ทันที (UI-16) |
| กระดาน 15 × 15 → 19 × 27 | ทำแล้ว 19 แถว × 27 คอลัมน์ ช่องพิเศษกระจายตามสัดส่วน (UI-14) |
| กด exchange แล้วมันจบเกมเลย | เล่นคนเดียว = BUG-8, คู่แข่งหลุด = BUG-22, เกม 3 คนขึ้นไป pass/exchange 4 ครั้ง = BUG-21 |
| กดคำไปแล้วจบเลย | เล่นคนเดียว = BUG-8, คู่แข่งหลุด = BUG-22 |
| เล่นคนเดียวไม่ได้ บังคับ win เลย | แก้ BUG-8 (LB-08, UI-24) |
| คำไม่ถูกไม่ต้องขึ้น Confirm Move | ทำแล้ว: คำผิด/กำลังตรวจจะไม่มีปุ่ม Confirm Move และป้ายขึ้น "Invalid word" / "Checking..." (UI-05) |
| ซูมไม่ตามเคอร์เซอร์เมาส์ ซูมแปลก ๆ | แก้ BUG-23 (ซูมไม่ตามเมาส์) และ BUG-24 (ซูมค้าง) (UI-21, UI-22) |
| ลากไปข้างบนแล้วตัวซ้ำ | แก้ BUG-25 (UI-25) |
| อยากให้มีบอกเลขห้อง | ทำแล้ว: "PIN xxxxxx" บนแถบบนของเกม กดเพื่อคัดลอก (UI-28) |
| มีโหมดผู้ชม | ทำแล้ว: Watch as spectator ใช้แค่ PIN (หัวข้อ 8.1) |
| คำลอยซ้อนกันสองอัน | แถบบนมือถือทับกัน = BUG-26, ข้อความแจ้งเตือนทับกัน = BUG-27 |
| เปลี่ยนการซูมทีละ 10 เป็นเท่าไหร่ก็ได้ | ทำแล้ว: ล้อ/trackpad ซูมตามระยะที่เลื่อน, ถ่างสองนิ้วบนมือถือ, ปุ่ม +/- ทีละ 20% (UI-29, UI-30) |

## กติกาที่ยังไม่ได้ทำ

ส่วนต่อไปนี้มีใน `Unified_Online_Crossword_Rules.md` แต่เกมยังไม่รองรับ (บางข้อขัดกับระบบเฉพาะของเกมนี้ เช่น HP การ์ด และช่อง ⚡ ควรตัดสินใจก่อนทำ)

- ช่อง Double/Triple Word และดาวกลางนับเป็น Double Word รวมถึงตัวคูณคำซ้อน ×4/×9 — ตอนนี้มีแค่ 2L/3L และ ⚡
- ขนาดกระดาน — ไฟล์กติกาเป็น 15 × 15 แต่ทีมขอ 19 × 27 จึงใช้ 19 × 27
- หักคะแนนตัวอักษรที่เหลือในแร็กตอนจบเกม และโบนัสให้คนที่วางหมดแร็ก
- นาฬิกาแบบหมากรุก 25 นาทีต่อคนและหักคะแนนเมื่อเกินเวลา — ตอนนี้ใช้เวลาต่อตาแบบ casual ตาม §7
- ระบบ challenge — ตอนนี้ตรวจคำอัตโนมัติจึงวางคำผิดไม่ได้ตั้งแต่แรก
- จั่วตัวอักษรเพื่อตัดสินคนเริ่มก่อน — ตอนนี้ host เริ่มก่อน
- กติกา six-zero — ตอนนี้เกมจบเมื่อไม่ได้คะแนนครบ 2 ตาต่อคนที่ยังเล่นอยู่ติดกัน (ขั้นต่ำ 4 ครั้ง, TN-10)
- จำนวนผู้เล่นสูงสุด 4 คน — ตอนนี้รับได้ 6 คน
